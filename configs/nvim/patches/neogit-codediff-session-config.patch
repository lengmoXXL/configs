diff --git a/lua/neogit/integrations/codediff.lua b/lua/neogit/integrations/codediff.lua
index 317acec0..8dd71237 100644
--- a/lua/neogit/integrations/codediff.lua
+++ b/lua/neogit/integrations/codediff.lua
@@ -72,15 +72,18 @@ local function make_explorer_data(status_result, focus_file)
 end
 
 local function open_explorer(view, git_root, status_result, original_revision, modified_revision, focus_file)
+  local path = require("codediff.core.path")
   ---@type table
   local session_config = {
-    mode = "explorer",
+    panel = {
+      name = "explorer",
+      data = make_explorer_data(status_result, focus_file),
+    },
     git_root = git_root,
-    original_path = "",
-    modified_path = "",
+    original = path.empty(),
+    modified = path.empty(),
     original_revision = original_revision,
     modified_revision = modified_revision,
-    explorer_data = make_explorer_data(status_result, focus_file),
   }
 
   view.create(session_config, "")
@@ -343,10 +346,10 @@ function M.open(section_name, item_name, opts)
 
       ---@type table
       local session_config = {
-        mode = "standalone",
+        panel = nil,
         git_root = git_root,
-        original_path = relative_path,
-        modified_path = relative_path,
+        original = require("codediff.core.path").make_ref(relative_path, git_root),
+        modified = require("codediff.core.path").make_ref(relative_path, git_root),
         original_revision = original_revision,
         modified_revision = modified_revision,
         conflict = true,
